# 不强制换行符转换，保留文件原有格式
* text=auto eol=lf

# Prisma 生成产物按原样提交，避免 Windows / WSL 混用时触发 CRLF/LF 归一化冲突
backend/prisma/generated/client/** -text

# 环境变量文件保持原始换行符，避免提交时触发 CRLF/LF 转换
backend/.env -text

# 明确指定某些二进制文件不进行转换
*.png binary
*.jpg binary
